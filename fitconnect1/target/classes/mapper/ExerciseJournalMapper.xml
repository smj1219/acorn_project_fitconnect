<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ExerciseJournal">
	<sql id="column">
		e_journal_id, exercise_id, exercise_order, exercise_set, exercise_count, exercise_weight, TO_CHAR(duration, 'YYYY.MM.DD') as duration
	</sql>
	
	<select id="getList" resultType="exerciseJournalDto" parameterType="exerciseJournalDto">
		SELECT <include refid="column"/>
		FROM exercisejournal
		WHERE m_calendar_id =(SELECT m_calendar_id FROM membercalendar 
			WHERE member_num=#{memberNum})
		ORDER BY exercise ASC
		
	</select>
	<select id="getExerOne" resultType="exerciseJournalDto" parameterType="int">
		SELECT <include refid="column"/>
		FROM exercisejournal
		WHERE e_journal_id=#{journalId}
	
	</select>
	<insert id="insert" parameterType="exerciseJournalDto">
		INSERT INTO exercisejournal
		(e_journal_id, exercise_id, exercise_order, exercise_set, exercise_count, exercise_weight, duration)
		VALUES(exercisejournal_seq.NEXTVAL, #{exerciseId}, #{exerciseOrder}, #{exerciseSet}, #{exerciseCount}, #{exerciseWeight}, SYSDATE)
	</insert>
	<update id="update" parameterType="exerciseJournalDto">
		UPDATE exercisejournal
		SET exercise_id=#{exerciseId}, exercise_order=#{exerciseOrder}, exercise_set=#{exerciseSet}, exercise_count=#{exerciseCount}, 
		exercise_weight=#{exerciseWeight}
		WHERE e_journal_id=#{journalId}
	</update>
	<delete id="delete" parameterType="int">
		DELETE FROM exercisejournal
		WHERE e_journal_id=#{journalId}
	</delete>

</mapper>